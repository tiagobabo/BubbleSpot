<div class="hide-on-phones">
 <div class = "navigation">
       <%= tabs_tag do |tab| %>
       <%= tab.home      "Home", root_path %>
       <%= tab.gestao      "Gestão", admins_index_path  %>
       <%= tab.shopping      "Shoppings", admins_shoppings_url  %>
       <%= tab.filmes      "Filmes do " + @shopping.nome,  admins_filmes_path(@shopping)  %>
       <%= tab.filme_new      "Novo Filme ",  new_filme_path(@shopping)  %>
       <% end %> 
    </div>
    </div>
<section>
		 <div class="row">
      <div class="twelve columns"> 
         <div class="underline"><h1>Novo Filme</h1></div>    
        <%= render 'form' %>
		 	</div>
		 </div>
    
</section>
