<div class="hide-on-phones">
 <div class = "navigation">
       <%= tabs_tag do |tab| %>
       <%= tab.home      "Home", root_path %>
       <%= tab.gestao      "Gestão", admins_index_path  %>
       <%= tab.shopping      "Shoppings", admins_shoppings_url  %>
       <%= tab.eventos      "Eventos do " + @shopping.nome,  admins_eventos_path(@shopping)  %>
       <%= tab.evento_edit     "Editar Evento ",  edit_evento_path(@shopping, @evento)  %>
       <% end %> 
    </div>
    </div>
<section>
		 <div class="row">
      <div class="twelve columns"> 
         <div class="underline"><h1>Editar Evento</h1></div>      
         <%= render 'form' %>
		 	</div>
		 </div>
   
</section>
